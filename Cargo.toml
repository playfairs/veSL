[package]
name = "vesl"
version = "0.1.0"
edition = "2024"
description = "PNG image carrier for arbitrary binary payloads"
license = "Unlicense"

[dependencies]
thiserror = "2"

[dev-dependencies]
tempfile = "3"
